@model BakeryOrderSystem.Models.Order

<h1>Добавление заказа</h1>

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">
        @TempData["Error"]
    </div>
}

<form asp-action="Create" method="post">

    <div class="mb-3">
        <label class="form-label">Клиент</label>
        <select name="CustomerId" asp-items="ViewBag.Customers" class="form-control"></select>
    </div>

    <div class="mb-3">
        <label class="form-label">Товар</label>
        <select name="ProductId" id="productSelect" class="form-control">
            @foreach (var product in ViewBag.Products)
            {
                <option value="@product.Id"
                        data-price="@product.Price"
                        data-stock="@product.StockQuantity">
                    @product.Name
                </option>
            }
        </select>
    </div>

    <div class="mb-3">
        <label class="form-label">Количество</label>
        <input name="Quantity"
               id="quantityInput"
               class="form-control"
               type="number"
               min="1"
               value="1"
               required />
    </div>

    <div class="alert alert-info">
        <div>Цена за штуку: <strong><span id="priceText">0</span> ₽</strong></div>
        <div>Остаток на складе: <strong><span id="stockText">0</span> шт.</strong></div>
        <div>Итого: <strong><span id="totalText">0</span> ₽</strong></div>
    </div>

    <div class="mb-3">
        <label class="form-label">Статус</label>
        <select name="Status" class="form-control">
            <option>Новый</option>
            <option>В работе</option>
            <option>Готов</option>
            <option>Выдан</option>
            <option>Отменен</option>
        </select>
    </div>

    <div class="mb-3">
        <label class="form-label">Примечание к заказу</label>
        <textarea name="Comment" class="form-control"></textarea>
    </div>

    <button type="submit" class="btn btn-primary">Сохранить</button>
    <a asp-action="Index" class="btn btn-secondary">Назад</a>
</form>

@section Scripts {
    <script>
        function updateOrderInfo() {
            const select = document.getElementById("productSelect");
            const quantityInput = document.getElementById("quantityInput");

            const selected = select.options[select.selectedIndex];

            const price = parseFloat(selected.getAttribute("data-price")) || 0;
            const stock = parseInt(selected.getAttribute("data-stock")) || 0;
            const quantity = parseInt(quantityInput.value) || 1;

            document.getElementById("priceText").innerText = price.toFixed(2);
            document.getElementById("stockText").innerText = stock;
            document.getElementById("totalText").innerText = (price * quantity).toFixed(2);
        }

        document.getElementById("productSelect").addEventListener("change", updateOrderInfo);
        document.getElementById("quantityInput").addEventListener("input", updateOrderInfo);

        updateOrderInfo();
    </script>
}